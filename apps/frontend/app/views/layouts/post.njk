{% extends "layouts/main.njk" %}

{% set pageName=md["meta"]["title"] %}

{% block beforeContent %}
{{ govukPhaseBanner({
  tag: {
    text: "Alpha"
  },
  html: 'This is a new service – your <a class="govuk-link" href="https://github.com/Samland-Gov/website/issues">feedback</a> will help us to improve it.'
}) }}
{{ govukBreadcrumbs({
  items: md["meta"]["breadcrums"]
}) }}

{% endblock %}

{% block content %}
<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-xl">
      {{ md["meta"]["title"] }}
    </h1>
  </div>
</div>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds govuk-prototype-kit-common-templates-mainstream-guide-body">
    {{ md["html"] | safe }}
  </div>

  <div class="govuk-grid-column-one-third">
    <aside class="govuk-prototype-kit-common-templates-related-items" role="complementary">
      <h2 class="govuk-heading-m" id="subsection-title">
        Related links
      </h2>
      <nav role="navigation" aria-labelledby="subsection-title">
        <ol class="govuk-list govuk-list--number govuk-!-font-size-16">
          {% for item in links %}
            <li>
              <a href="{{item["href"]}}">
                {{ item["text"] }}
              </a>
            </li>
          {% endfor %}
        </ol>
      </nav>
    </aside>
  </div>
</div>

{% endblock %}
